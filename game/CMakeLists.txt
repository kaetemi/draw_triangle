
FILE(GLOB SRCS *.cpp)
FILE(GLOB HDRS *.h)
FILE(GLOB INLS *.inl)
FILE(GLOB RSRC *.rc)

SOURCE_GROUP("" FILES ${SRCS} ${HDRS} ${INLS} ${RSRC})

ADD_EXECUTABLE(game
  ${SRCS}
  ${HDRS}
  ${INLS}
   ${RSRC}
)

TARGET_LINK_LIBRARIES(game PUBLIC
  gl3w
)
